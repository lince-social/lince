{
  "name": "lince-browser-tests",
  "private": true,
  "scripts": {
    "test:organ-discovery": "cargo run -q -p lince -- --help >/dev/null && playwright test tests/e2e/organ-discovery.spec.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
